global:
  scrape_interval: 15s # Set the scrape interval to every 15 seconds.

scrape_configs:
  - job_name: 'api-gateway'
    static_configs:
      # Use the service name defined in docker-compose.yaml for the API Gateway.
      - targets: ['api-gateway:8080'] 